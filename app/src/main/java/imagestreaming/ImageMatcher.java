package imagestreaming;

public class ImageMatcher {
    void onClientImageAvailable() {
        // takes client frame with timestamp, finds a leader frame with a matching timestamp

        // reports image pair to the depth estimator?
    }
}
